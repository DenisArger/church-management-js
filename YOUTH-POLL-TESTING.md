# Тестирование создания опроса на молодежное служение по расписанию

## Обзор

Система автоматического создания опросов для молодежного служения работает следующим образом:

1. **Netlify Scheduled Function** (`poll-scheduler`) запускается каждые 15 минут в каждые 15 минут
2. Функция проверяет наличие молодежного события на завтра в базе данных Notion
3. Если событие найдено, создается опрос в Telegram группе молодежи
4. Если события нет, функция завершается без создания опроса

## Способы тестирования

### 1. Ручное тестирование через Node.js скрипт

**Файл:** `test-youth-poll.js`

Этот скрипт позволяет протестировать функции напрямую без веб-хуков.

#### Использование:

```bash
# Сначала соберите проект
yarn build

# Запустите все тесты
node test-youth-poll.js

# Или отдельные тесты
node test-youth-poll.js manual      # Тест ручной команды
node test-youth-poll.js scheduled   # Тест запланированного выполнения
node test-youth-poll.js notion      # Тест Notion сервиса
node test-youth-poll.js env         # Проверка переменных окружения
```

#### Что тестирует:

- ✅ Настройку переменных окружения
- ✅ Подключение к Notion API
- ✅ Поиск молодежных событий на завтра
- ✅ Создание опроса через Telegram API
- ✅ Обработку ошибок

### 2. Тестирование Netlify функций

**Файл:** `test-auto-poll-scheduler.sh`

Этот скрипт тестирует Netlify функции как локально, так и в продакшене.

#### Использование:

```bash
# Проверить статус Netlify dev
bash test-auto-poll-scheduler.sh check-dev

# Тест локальной функции
bash test-auto-poll-scheduler.sh local

# Тест развернутой функции
bash test-auto-poll-scheduler.sh deployed https://f5fa2ef2--church-telegram-bot.netlify.live

# Симуляция cron триггера
bash test-auto-poll-scheduler.sh cron https://f5fa2ef2--church-telegram-bot.netlify.live/.netlify/functions/poll-scheduler

# Все тесты сразу
bash test-auto-poll-scheduler.sh all https://f5fa2ef2--church-telegram-bot.netlify.live
```

#### Что тестирует:

- ✅ HTTP статус ответа (200)
- ✅ Структуру JSON ответа
- ✅ Успешность выполнения функции
- ✅ Симуляцию реального cron триггера

### 3. Симуляция расписания (Cron Job)

**Файл:** `cron-simulation.sh`

Этот скрипт имитирует работу cron job для тестирования расписания.

#### Использование:

```bash
# Одноразовый триггер
bash cron-simulation.sh trigger http://localhost:8889/.netlify/functions/poll-scheduler

# Запуск периодического тестирования (каждые 30 минут)
bash cron-simulation.sh start http://localhost:8889/.netlify/functions/poll-scheduler 1800

# Запуск периодического тестирования (каждые 15 минут)
bash cron-simulation.sh start https://f5fa2ef2--church-telegram-bot.netlify.live/.netlify/functions/poll-scheduler 3600

# Остановка симуляции
bash cron-simulation.sh stop

# Проверка статуса
bash cron-simulation.sh status

# Просмотр логов
bash cron-simulation.sh logs 100
```

#### Особенности:

- 📝 Логирование всех операций в файл
- 🔄 Автоматическое повторение с заданным интервалом
- 🛑 Graceful shutdown при получении сигналов
- 📊 Мониторинг статуса выполнения

### 4. Тестирование через Telegram бота

#### Ручная команда:

```
/youth_poll
```

#### Что происходит:

1. Бот проверяет наличие молодежного события на завтра
2. Если событие найдено - создает опрос в группе молодежи
3. Если события нет - отправляет сообщение пользователю

### 5. Тестирование через веб-хук

**Файл:** `test-webhook.js`

```bash
node test-webhook.js
```

Отправляет тестовое сообщение на локальный отладочный сервер.

## Настройка тестовых данных

### Переменные окружения

Убедитесь, что в `.env` файле настроены:

```env
# Debug конфигурация для тестирования
TELEGRAM_BOT_TOKEN_DEBUG=your_debug_bot_token
TELEGRAM_CHAT_ID_DEBUG=-1002317620302
TELEGRAM_TOPIC_ID_DEBUG=78

# Notion конфигурация
NOTION_TOKEN=your_notion_token
NOTION_GENERAL_CALENDAR_DATABASE=your_calendar_database_id

# Debug режим
DEBUG=true
```

### Тестовые данные в Notion

Для тестирования создайте в базе данных календаря запись с:

- **Дата**: завтрашний день
- **Тип служения**: "Молодежное"
- **Название служения**: "Тестовое молодежное служение"
- **Описание**: "Описание тестового служения"
- **Тема** (если есть): "Тестовая тема"

## Пошаговое тестирование

### Шаг 1: Подготовка

```bash
# 1. Убедитесь, что Netlify dev запущен
netlify dev --live

# 2. Проверьте переменные окружения
node test-youth-poll.js env
```

### Шаг 2: Тест Notion сервиса

```bash
# Проверьте подключение к Notion и поиск событий
node test-youth-poll.js notion
```

### Шаг 3: Тест локальной функции

```bash
# Тест Netlify функции локально
bash test-auto-poll-scheduler.sh local
```

### Шаг 4: Тест развернутой функции

```bash
# Тест в продакшене
bash test-auto-poll-scheduler.sh deployed https://f5fa2ef2--church-telegram-bot.netlify.live
```

### Шаг 5: Симуляция расписания

```bash
# Запустите симуляцию cron job
bash cron-simulation.sh start http://localhost:8889/.netlify/functions/poll-scheduler 300

# Проверьте логи
bash cron-simulation.sh logs 50
```

## Мониторинг и отладка

### Логи Netlify

Логи отображаются в терминале где запущен `netlify dev --live`:

```
[netlify] Function poll-scheduler executed
[netlify] Response: {"success": true, "message": "Poll scheduler completed successfully"}
```

### Логи симуляции

Логи сохраняются в `cron-simulation.log`:

```
[2024-01-15 каждые 15 минут:00] Triggering Poll scheduler at http://localhost:8889/.netlify/functions/poll-scheduler
[2024-01-15 каждые 15 минут:01] Response: HTTP 200
[2024-01-15 каждые 15 минут:01] SUCCESS: Poll scheduler executed successfully
```

### Проверка статуса

```bash
# Проверка статуса симуляции
bash cron-simulation.sh status

# Проверка статуса веб-хука
bash test-bot-webhook.sh info
```

## Устранение проблем

### Проблема: "No youth event found for tomorrow"

**Причина:** В Notion нет молодежного события на завтра

**Решение:**

1. Создайте тестовое событие в Notion на завтра
2. Убедитесь, что тип служения = "Молодежное"
3. Проверьте правильность даты

### Проблема: "502 Bad Gateway"

**Причина:** Netlify dev сервер не запущен

**Решение:**

```bash
netlify dev --live
```

### Проблема: "Failed to create youth poll"

**Причина:** Проблемы с Telegram API

**Решение:**

1. Проверьте токен бота
2. Убедитесь, что бот добавлен в группу
3. Проверьте права бота в группе

### Проблема: "Notion API error"

**Причина:** Проблемы с подключением к Notion

**Решение:**

1. Проверьте токен Notion
2. Убедитесь в правильности ID базы данных
3. Проверьте права доступа к базе данных

## Автоматическое тестирование

### Настройка CI/CD

Можно добавить автоматические тесты в GitHub Actions:

```yaml
name: Test Poll scheduler
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: "18"
      - run: yarn install
      - run: yarn build
      - run: node test-youth-poll.js
        env:
          TELEGRAM_BOT_TOKEN_DEBUG: ${{ secrets.TELEGRAM_BOT_TOKEN_DEBUG }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          # ... другие переменные
```

## Заключение

Используйте эти инструменты для комплексного тестирования системы создания опросов на молодежное служение. Начните с ручного тестирования, затем переходите к автоматизированным тестам и симуляции расписания.

