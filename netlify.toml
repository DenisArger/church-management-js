[build]
  command = "yarn build"
  functions = "netlify/functions"
  publish = "dist"

[dev]
  port = 8888
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# Scheduled function for polls and notifications
# Runs every 15 minutes
[functions."poll-scheduler"]
  schedule = "*/15 * * * *"

