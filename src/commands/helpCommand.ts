import { CommandResult } from "../types";
import { sendMessage } from "../services/telegramService";
import { logInfo } from "../utils/logger";

export const executeHelpCommand = async (
  userId: number,
  chatId: number
): Promise<CommandResult> => {
  logInfo("Executing help command", { userId, chatId });

  const helpMessage = `
🤖 <b>Доступные команды бота:</b>

<b>📋 Основные команды:</b>
• <code>/request_pray</code> - Отправить молитвенную рассылку
• <code>/request_pray date</code> - Рассылка с сортировкой по дате
• <code>/request_pray name</code> - Рассылка с сортировкой по имени
• <code>/add_prayer</code> - Добавить молитвенную запись на неделю
• <code>/daily_scripture</code> - Получить ежедневное писание
• <code>/create_poll</code> - Создать опрос
• <code>/youth_poll</code> - Создать опрос о молодежном служении
• <code>/request_state_sunday</code> - Информация о воскресном служении
• <code>/weekly_schedule</code> - Расписание служений на предстоящую неделю
• <code>/prayer_week</code> - Информация о молитвах на следующую неделю

<b>🔧 Служебные команды:</b>
• <code>/test_notion</code> - Проверить подключение к Notion
• <code>/help</code> - Показать это сообщение

<b>📝 Молитвенные нужды:</b>
Просто напишите сообщение с молитвенной нуждой, и бот автоматически её запишет.

<b>⚙️ Параметры команды /request_pray:</b>
• <code>date</code> или <code>дата</code> - сортировка по дате (по умолчанию)
• <code>name</code> или <code>имя</code> - сортировка по алфавиту

<b>📝 Команда /add_prayer:</b>
Формат: <code>Имя | Тема | Неделя (current/next)</code>
• <code>/add_prayer</code> - показать справку по формату
• <code>/add_prayer Иван Петров | Здоровье | current</code>

<b>📊 Примеры использования:</b>
• <code>/request_pray</code> - стандартная рассылка
• <code>/request_pray date</code> - сортировка по дате
• <code>/request_pray name</code> - сортировка по имени
• <code>/add_prayer Мария Сидорова | Работа | next</code>

<b>🔐 Права доступа:</b>
Все команды доступны только авторизованным пользователям.

<b>❓ Нужна помощь?</b>
Если у вас возникли проблемы, обратитесь к администратору.
`;

  return await sendMessage(chatId, helpMessage, { parse_mode: "HTML" });
};
